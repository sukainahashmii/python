#range is a function returns a sequence of numbers startinf from 0 by default, increment by 1 by default and stops before a specified number
for el in range(6): #6 wont be included
    print(el)


for el in range(1,11,2): #this follows the format of (start?, stop, step?)
    print(el)
    
